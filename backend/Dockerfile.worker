FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir httpx

COPY simulator_worker.py .

# Variables requeridas en Railway:
# SIEM_API_URL, SIEM_USERNAME, SIEM_PASSWORD, SIM_RATE
CMD ["python", "-u", "simulator_worker.py"]
